#include <stdio.h>

/**
 * main - prints specified string
 * Return: return 0 to whomever called it
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
